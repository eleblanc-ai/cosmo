# Ignore everything at the root
/*

# Except the cosmo directory, README, and this file
!cosmo/
!img/
!README.md
!.gitignore


# Include all files in cosmo by default
!cosmo/**

# But ignore all files in slices except .gitkeep
cosmo/slices/*
!cosmo/slices/.gitkeep

# Ignore runtime state files
cosmo/current-phase.md
cosmo/current-plan.md

# Local development only — not part of template
reset.sh
